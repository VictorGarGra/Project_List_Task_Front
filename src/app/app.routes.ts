// src/app/app.routes.ts

import { Routes } from '@angular/router';

export const routes: Routes = [
  // Dejamos el arreglo vacío por ahora, o listo para futuras rutas
  // que no sean la principal.
  // Por ejemplo: { path: 'otra-pagina', component: OtroComponente }
];
